# 2026-04-28 HonoAPI 分销商与可分配额度系统设计开发总结

## 1. 文档目的

本文记录本次 HonoAPI 基于 New API 项目新增“分销商与可分配额度系统”的设计、开发、调整、验证、上线准备与后期维护要点，便于后续维护、升级、同步上游 main、迁移生产环境和继续二次开发。

本次开发分支：

```text
distributor
```

当前主要提交：

```text
e247c299 Add distributor management system
e88d1cd0 Allow custom feature image tags
```

镜像构建目标：

```text
ghcr.io/honoacc/honoapi:distributor-v1
```

---

## 2. 业务背景与目标

原 New API 项目已有用户、额度、充值、令牌、渠道、日志等能力，但没有面向代理/分销场景的层级关系与可再分配额度管理能力。

本次目标是在尽量不侵入 relay、计费、模型调用和现有管理员用户管理逻辑的前提下，新增独立的分销管理系统，使超级管理员、高级分销商、分销商可以按权限管理直属下级，并向下级分配系统额度或可分配金额。

核心要求：

1. 新增分销角色，但不能让分销角色获得管理员权限。
2. 新增独立分销管理页面，不混入原用户管理危险操作。
3. 统一业务入口为“添加下级”。
4. 新增下级默认必须是普通用户。
5. 后续再由有权限的操作者调整直属下级角色。
6. 额度/金额分配必须防止并发超发。
7. 支持现有 SQLite 部署自动迁移。
8. 前端展示尽量贴近用户管理页面风格。
9. 保留 New API 项目归属信息，同时增加 Hono API 展示品牌。

---

## 3. 最终业务规则

### 3.1 角色定义

新增角色：

```go
RoleDistributorUser       = 7
RoleSeniorDistributorUser = 8
```

现有关键角色：

```go
RoleCommonUser = 1
RoleAdminUser  = 10
RoleRootUser   = 100
```

角色含义：

| role | 名称 | 说明 |
|---|---|---|
| 1 | 普通用户 | 可作为下级，接收系统额度 |
| 7 | 分销商 | 可添加普通用户下级，可给普通用户分配系统额度 |
| 8 | 高级分销商 | 可添加普通用户下级，可将直属普通用户设为分销商，可分配系统额度或可分配金额 |
| 10 | 普通管理员 | 保持原管理员能力，但默认不能访问分销管理 |
| 100 | 超级管理员/root | 可访问分销管理，可添加直属下级，可设置高级分销商/分销商/普通用户，可分配额度/金额 |

### 3.2 权限边界

#### 超级管理员 root

root 可以：

1. 在分销管理中直接“添加下级”。
2. root 直接添加的下级关系写为：
   ```text
   parent_user_id = 0
   created_by = rootId
   ```
3. root 可将自己的直属下级设置为：
   - 高级分销商
   - 分销商
   - 普通用户
4. root 可查看所有已绑定的上下级关系。
5. root 可查看所有操作记录。
6. root 给普通用户添加的是系统额度。
7. root 给分销商/高级分销商添加的是可分配金额。
8. root 分配额度/金额不扣减自身额度。

#### 高级分销商

高级分销商可以：

1. 添加普通用户为直属下级。
2. 将直属普通用户设置为分销商。
3. 将直属分销商/普通用户设置为普通用户。
4. 给直属普通用户添加系统额度。
5. 给直属分销商添加可分配金额。
6. 只能操作自己的直属下级。
7. 分配额度/金额会扣减自己的可分配金额。

高级分销商不能：

1. 设置高级分销商。
2. 操作非直属下级。
3. 给非直属用户分配额度。

#### 分销商

分销商可以：

1. 添加普通用户为直属下级。
2. 给直属普通用户添加系统额度。
3. 查看自己的分销管理页面和操作记录。

分销商不能：

1. 调整下级角色。
2. 设置分销商或高级分销商。
3. 给分销商/高级分销商分配可分配金额。
4. 操作非直属下级。

#### 普通用户

普通用户：

1. 不显示“分销管理”入口。
2. 不能访问分销 API。

#### 普通管理员 role=10

普通管理员：

1. 保持原后台管理员能力。
2. 默认不显示“分销管理”。
3. 不能访问分销 API。

---

## 4. 数据模型设计

### 4.1 users 表新增字段

文件：

```text
model/user.go
```

新增字段：

```go
DistributableQuota int `json:"distributable_quota" gorm:"type:int;default:0"`
```

含义：

- 用户可继续向下级分配的额度余额。
- 前端按金额展示，底层仍沿用 New API 原生 quota 整数单位。
- root 分配不扣自身 `distributable_quota`。
- 高级分销商/分销商分配时会扣减自身 `distributable_quota`。

### 4.2 distributor_relations 表

文件：

```text
model/distributor.go
```

结构：

```go
type DistributorRelation struct {
    Id           int    `json:"id" gorm:"primaryKey"`
    ParentUserId int    `json:"parent_user_id" gorm:"index"`
    ChildUserId  int    `json:"child_user_id" gorm:"uniqueIndex"`
    Remark       string `json:"remark" gorm:"type:text"`
    CreatedBy    int    `json:"created_by"`
    CreatedAt    int64  `json:"created_at" gorm:"autoCreateTime"`
    UpdatedAt    int64  `json:"updated_at" gorm:"autoUpdateTime"`
}
```

关键约束：

- `child_user_id` 唯一，保证一个用户只能有一个直接上级。
- `parent_user_id=0` 表示 root 直属下级。
- `created_by` 用于记录关系创建者，特别用于 root 直属下级场景。

### 4.3 distributor_quota_logs 表

记录额度/金额分配流水。

结构：

```go
type DistributorQuotaLog struct {
    Id              int    `json:"id" gorm:"primaryKey"`
    OperatorUserId  int    `json:"operator_user_id" gorm:"index"`
    SourceUserId    int    `json:"source_user_id" gorm:"index"`
    TargetUserId    int    `json:"target_user_id" gorm:"index"`
    Amount          int    `json:"amount"`
    SourceBefore    int    `json:"source_before"`
    SourceAfter     int    `json:"source_after"`
    TargetBefore    int    `json:"target_before"`
    TargetAfter     int    `json:"target_after"`
    TargetQuotaType string `json:"target_quota_type" gorm:"type:varchar(32)"`
    Action          string `json:"action" gorm:"type:varchar(64);index"`
    Remark          string `json:"remark" gorm:"type:text"`
    CreatedAt       int64  `json:"created_at" gorm:"autoCreateTime;index"`
}
```

目标额度类型：

```go
DistributorQuotaTargetDistributable = "distributable_quota"
DistributorQuotaTargetUserQuota     = "quota"
```

### 4.4 distributor_relation_logs 表

记录关系和角色调整操作。

结构：

```go
type DistributorRelationLog struct {
    Id             int    `json:"id" gorm:"primaryKey"`
    OperatorUserId int    `json:"operator_user_id" gorm:"index"`
    ParentUserId   int    `json:"parent_user_id" gorm:"index"`
    ChildUserId    int    `json:"child_user_id" gorm:"index"`
    Action         string `json:"action" gorm:"type:varchar(64);index"`
    Remark         string `json:"remark" gorm:"type:text"`
    CreatedAt      int64  `json:"created_at" gorm:"autoCreateTime;index"`
}
```

角色调整日志使用：

```text
action = set_role
remark = role:oldRole->newRole
```

前端会解析为通俗中文，例如：

```text
调整角色：普通用户 → 分销商
```

---

## 5. 后端实现总结

### 5.1 角色与鉴权

文件：

```text
common/constants.go
middleware/auth.go
```

新增 helper：

```go
func IsDistributorRole(role int) bool
func IsSeniorDistributorRole(role int) bool
func IsRootRole(role int) bool
func CanAccessDistributorConsole(role int) bool
```

新增 middleware：

```go
RootOrDistributorAuth()
```

关键原则：

- 分销 API 允许 root、分销商、高级分销商访问。
- `AdminAuth()` 保持原语义，不让 role=7/8 成为管理员。

### 5.2 路由

文件：

```text
router/api-router.go
```

新增路由组：

```text
/api/distributor
```

主要接口：

```text
GET  /api/distributor/summary
GET  /api/distributor/children
GET  /api/distributor/search-bindable-users
POST /api/distributor/bind
POST /api/distributor/grant-quota
POST /api/distributor/set-role
PUT  /api/distributor/relation/:child_user_id/remark
GET  /api/distributor/quota-logs
GET  /api/distributor/operation-logs
```

目前仍保留一个历史遗留接口：

```text
GET /api/distributor/search-role-candidates
```

该接口来自早期 root 单独“设置分销角色”方案，当前前端不再使用。后期可删除。

### 5.3 业务 service

文件：

```text
service/distributor.go
```

核心函数：

```go
GetDistributorSummary(...)
ListDistributorChildren(...)
SearchBindableUsers(...)
BindDistributorChild(...)
SetDistributorRole(...)
UpdateDistributorRemark(...)
GrantDistributorQuota(...)
ListDistributorQuotaLogs(...)
ListDistributorOperationLogs(...)
```

### 5.4 关系绑定规则

`BindDistributorChild` 中：

1. 使用事务。
2. 解析操作者实际 parent。
3. 检查 child 必须是普通用户。
4. 检查 child 尚未绑定上级。
5. 写入 `distributor_relations`。
6. 写入 `distributor_relation_logs`。

最终规则：

```text
所有新添加下级默认必须是普通用户。
```

### 5.5 角色调整规则

`SetDistributorRole` 中：

1. 只能设置为：普通用户、分销商、高级分销商。
2. 不能修改 root/admin。
3. 只能修改已绑定关系中的用户。
4. root 可设置自己的 root 直属下级。
5. 高级分销商只能设置直属下级为分销商/普通用户。
6. 分销商无权修改角色。
7. 写入角色调整日志。

### 5.6 额度/金额分配规则

`GrantDistributorQuota` 中：

1. `amount` 必须大于 0。
2. 全流程使用事务。
3. root 不扣自身额度。
4. 非 root 使用条件更新扣减：

```go
WHERE id = ? AND distributable_quota >= ?
```

并检查：

```go
RowsAffected == 1
```

5. 根据目标角色决定增加字段：

| 目标角色 | 增加字段 | 前端含义 |
|---|---|---|
| 普通用户 | quota | 添加额度 |
| 分销商 | distributable_quota | 添加金额 |
| 高级分销商 | distributable_quota | 添加金额 |

---

## 6. 前端实现总结

### 6.1 新增页面

文件：

```text
web/src/pages/Distributor/index.jsx
```

路由：

```text
/console/distributor
```

页面内容：

1. 顶部统计卡片。
2. 分销列表。
3. 添加下级。
4. 添加额度/金额。
5. 备注。
6. 操作记录。
7. 角色调整按钮。

### 6.2 新增 hooks

文件：

```text
web/src/hooks/distributors/useDistributorData.jsx
```

负责：

- 加载 summary
- 加载 children
- 搜索可绑定用户
- 绑定下级
- 添加额度/金额
- 修改备注
- 设置角色
- 加载操作记录

### 6.3 新增组件目录

```text
web/src/components/table/distributors/
```

主要文件：

```text
DistributorsActions.jsx
DistributorsColumnDefs.jsx
DistributorsDescription.jsx
DistributorsFilters.jsx
DistributorsTable.jsx
modals/BindChildModal.jsx
modals/EditRemarkModal.jsx
modals/GrantQuotaModal.jsx
modals/QuotaLogsModal.jsx
modals/SetDistributorRoleModal.jsx
```

注意：

`SetDistributorRoleModal.jsx` 是早期方案遗留文件，当前前端实际不再走“独立设置分销角色”弹窗。后期可删除。

### 6.4 表格展示逻辑

当前列包括：

- 用户
- 上级，仅 root 显示
- 角色
- 状态
- 已分配金额
- 已分配额度
- 备注
- 操作

最新调整：

1. root 视角只显示用户上级，不显示下级。
2. 高级分销商/分销商视角删除上级/下级列。
3. 分销列表用户列只展示用户名。

### 6.5 额度展示

本次根据用户反馈调整为：

- 不按 token 概念展示。
- 使用 `$` 金额方式展示。
- 参考用户管理里的额度/金额换算逻辑。
- 输入框默认值改为 `1`。
- 删除“系统将按当前汇率换算...”提示。

底层仍写入 New API 原生 quota 整数值，前端负责金额与 quota 的转换显示。

### 6.6 侧边栏

文件：

```text
web/src/components/layout/SiderBar.jsx
web/src/hooks/common/useSidebar.js
```

调整：

- “分销管理”放到“个人中心”下。
- 位于个人中心最下面。
- 仅 root / 高级分销商 / 分销商显示。

### 6.7 用户管理角色显示

文件：

```text
web/src/components/table/users/UsersColumnDefs.jsx
```

新增：

- role=7 显示“分销商”
- role=8 显示“高级分销商”

解决用户管理中显示“未知身份”的问题。

---

## 7. 品牌展示调整

用户希望将站点展示为 Hono API。

由于项目规则保护 New API / QuantumNous 项目信息，不直接删除 New API 归属，而采用兼容方式：

1. 页面展示品牌为：

```text
Hono API
```

2. logo/favicon 使用：

```text
web/public/honoapi.svg
```

来源：

```text
/Users/pobeno/PobENo/CCD/honoapi.svg
```

3. 暗色模式自动反色：

```css
html.dark .hono-site-logo {
  filter: invert(1);
}
```

4. 页脚显示：

```text
© 2026 Hono API. 版权所有
基于 New API 开发
```

其中 `New API` 文字保留链接：

```text
https://github.com/QuantumNous/new-api
```

---

## 8. GitHub Actions 镜像构建

### 8.1 工作流文件

```text
.github/workflows/docker-image-feature.yml
```

原本只能用分支名作为 tag。

本次新增 `image_tag` 输入参数，支持自定义 tag：

```yaml
image_tag:
  description: 'Optional custom image tag, e.g. distributor-v1 or ghcr.io/honoacc/honoapi:distributor-v1'
  required: false
  default: ''
```

### 8.2 已触发构建

已通过 GitHub CLI 触发：

```text
branch = distributor
image_tag = distributor-v1
```

Actions 地址：

```text
https://github.com/HonoACC/HonoAPI/actions/runs/25032381674
```

构建成功后会推送：

```text
ghcr.io/honoacc/honoapi:distributor-v1
```

同时生成带日期和 commit 的版本 tag，例如：

```text
ghcr.io/honoacc/honoapi:distributor-v1-20260428-e88d1cd0
```

---

## 9. 验证记录

后端验证：

```bash
go test ./service ./controller ./router
```

结果：通过。

前端验证：

```bash
cd web
NODE_OPTIONS=--max-old-space-size=4096 bun run build
```

结果：通过。

构建存在非阻塞警告：

1. browserslist 数据较旧。
2. lottie-web 使用 eval。
3. 部分 chunk 大于 500kB。

这些均非本次功能上线阻塞。

---

## 10. SQLite 迁移说明

服务器当前部署为 New API + SQLite。

本次迁移会自动执行 GORM AutoMigrate：

1. `users` 表新增字段：

```text
distributable_quota
```

2. 新增表：

```text
distributor_relations
distributor_quota_logs
distributor_relation_logs
```

3. 新增索引，包括：

```text
distributor_relations.child_user_id unique index
```

### 10.1 迁移风险

本次 SQLite 迁移风险较低，原因：

1. 只新增字段和表。
2. 不删除旧字段。
3. 不修改旧表字段类型。
4. 不迁移旧数据。
5. 旧版程序通常会忽略额外表和额外字段。

但仍必须在生产部署前备份 SQLite 数据库。

### 10.2 推荐上线前备份

假设数据目录：

```text
/opt/new-api/data
```

备份：

```bash
cd /opt/new-api
tar -czf new-api-data-backup-$(date +%F-%H%M%S).tar.gz data
```

或备份 db 文件：

```bash
cp /opt/new-api/data/new-api.db /opt/new-api/data/new-api.db.bak-$(date +%F-%H%M%S)
```

完整性检查：

```bash
sqlite3 /opt/new-api/data/new-api.db "PRAGMA integrity_check;"
```

期望输出：

```text
ok
```

### 10.3 推荐先用副本预演

```bash
cp -a /opt/new-api/data /opt/new-api/data-migration-test
```

启动测试容器：

```bash
docker run --rm \
  --name honoapi-migration-test \
  -p 3001:3000 \
  -v /opt/new-api/data-migration-test:/data \
  ghcr.io/honoacc/honoapi:distributor-v1
```

确认：

```bash
sqlite3 /opt/new-api/data-migration-test/new-api.db ".tables"
sqlite3 /opt/new-api/data-migration-test/new-api.db "PRAGMA table_info(users);"
```

应看到：

```text
distributor_relations
distributor_quota_logs
distributor_relation_logs
distributable_quota
```

---

## 11. 生产部署建议

建议低峰期操作。

### 11.1 拉取镜像

```bash
docker pull ghcr.io/honoacc/honoapi:distributor-v1
```

更推荐使用 Actions 生成的具体版本 tag：

```bash
docker pull ghcr.io/honoacc/honoapi:distributor-v1-20260428-e88d1cd0
```

### 11.2 停止旧容器

```bash
docker stop new-api
docker rename new-api new-api-before-distributor-$(date +%F-%H%M%S)
```

### 11.3 启动新容器

示例：

```bash
docker run -d \
  --name new-api \
  --restart always \
  -p 3000:3000 \
  -v /opt/new-api/data:/data \
  ghcr.io/honoacc/honoapi:distributor-v1
```

实际部署时必须复用旧容器的：

- 端口
- volume
- 环境变量
- network
- restart 策略
- 反代配置

### 11.4 上线后验证

root 登录后验证：

1. `/console/distributor` 可访问。
2. 添加普通用户为下级。
3. 将直属下级设置为高级分销商/分销商。
4. 给普通用户添加额度。
5. 给分销商/高级分销商添加金额。
6. 查看操作记录。

高级分销商验证：

1. 能添加普通用户下级。
2. 能把直属普通用户设置为分销商。
3. 能给普通用户添加额度。
4. 能给分销商添加金额。

分销商验证：

1. 能添加普通用户下级。
2. 不能调整角色。
3. 只能给普通用户添加额度。

普通用户验证：

1. 看不到分销管理。
2. 不能访问分销 API。

---

## 12. 回滚方案

### 12.1 只回滚容器

如果新版本异常但数据库可继续使用：

```bash
docker stop new-api
docker rm new-api
docker rename new-api-before-distributor-时间戳 new-api
docker start new-api
```

因为本次数据库只新增字段和表，旧版 New API 通常可以忽略这些额外结构。

### 12.2 完整回滚数据库

如需完全回滚：

```bash
docker stop new-api
mv /opt/new-api/data /opt/new-api/data-after-failed-migration
tar -xzf /opt/new-api/new-api-data-backup-xxxx.tar.gz -C /opt/new-api/
docker start new-api
```

---

## 13. 已知遗留与后续优化建议

### 13.1 删除历史遗留接口和文件

当前可后期删除：

```text
GET /api/distributor/search-role-candidates
service.SearchDistributorRoleCandidates
controller.SearchDistributorRoleCandidates
web/src/components/table/distributors/modals/SetDistributorRoleModal.jsx
```

这些来自早期 root 单独设置分销角色方案，当前业务已经统一为“添加下级 → 再设置角色”。

### 13.2 操作记录性能优化

当前：

```go
ListDistributorOperationLogs(...)
```

在 Go 内存中合并 quota logs 和 relation logs，再排序分页。

初期可用，但数据量变大后 root 查看全部记录可能慢。

后期建议：

1. SQL 层分页。
2. 增加默认时间范围。
3. 使用兼容 SQLite/MySQL/PostgreSQL 的 union 查询。
4. 或拆分额度记录与关系记录分页后再合并。

### 13.3 增加自动化测试

建议补充：

1. root 添加直属下级。
2. root 设置高级分销商。
3. 高级分销商设置分销商。
4. 分销商不能设置角色。
5. 非直属下级不能操作。
6. 非 root 分配额度并发防超发。
7. 普通管理员不能访问分销接口。
8. SQLite AutoMigrate 检查。

### 13.4 前端 UI 优化

当前页眉导航只是剩余空间内居中，不是真正页面级居中。

后期建议改为三列 grid：

```text
左：Logo
中：导航
右：操作按钮
```

### 13.5 品牌配置优化

当前 Hono API 展示品牌是前端硬编码：

```js
SITE_DISPLAY_NAME = 'Hono API'
SITE_DISPLAY_LOGO = '/honoapi.svg'
```

后期可改为：

1. 后台配置。
2. 环境变量。
3. 构建参数。
4. 保留 New API 归属信息。

### 13.6 上游同步冲突风险

后续同步官方 `main` 时，容易冲突的文件：

```text
common/constants.go
middleware/auth.go
model/user.go
model/main.go
router/api-router.go
web/src/App.jsx
web/src/components/layout/SiderBar.jsx
web/src/helpers/utils.jsx
web/src/hooks/common/useSidebar.js
web/src/components/table/users/UsersColumnDefs.jsx
.github/workflows/docker-image-feature.yml
```

分销独立新增文件通常冲突风险较低。

---

## 14. 关键文件清单

### 后端

```text
common/constants.go
middleware/auth.go
model/user.go
model/distributor.go
model/main.go
service/distributor.go
controller/distributor.go
router/api-router.go
```

### 前端

```text
web/src/pages/Distributor/index.jsx
web/src/hooks/distributors/useDistributorData.jsx
web/src/components/table/distributors/
web/src/components/table/users/UsersColumnDefs.jsx
web/src/App.jsx
web/src/components/layout/SiderBar.jsx
web/src/hooks/common/useSidebar.js
web/src/helpers/auth.jsx
web/src/helpers/render.jsx
web/src/helpers/utils.jsx
web/src/index.css
web/public/honoapi.svg
```

### GitHub Actions

```text
.github/workflows/docker-image-feature.yml
```

---

## 15. 当前总体进度

已完成：

1. 角色体系设计。
2. 后端模型设计。
3. SQLite 兼容自动迁移。
4. 分销关系绑定。
5. 角色调整。
6. 额度/金额分配。
7. 操作记录。
8. root 全局查看关系和记录。
9. 前端分销管理页面。
10. 用户管理角色显示。
11. 侧边栏入口调整。
12. 品牌展示调整。
13. 前端构建验证。
14. 后端关键包测试。
15. distributor 分支提交并推送。
16. GitHub Actions 自定义镜像 tag 支持。
17. 已触发 `distributor-v1` 镜像构建。

未完成 / 后期建议：

1. 删除早期遗留接口和 SetDistributorRoleModal 文件。
2. 优化操作记录大数据量分页性能。
3. 增加更系统的单元测试和集成测试。
4. 生产 SQLite 副本预演迁移。
5. 生产低峰期灰度上线。
6. 上线后观察日志与分销业务数据。

---

## 16. 维护建议

后续开发前建议先确认：

1. 当前分支是否已同步 upstream/main。
2. 是否有官方角色体系或用户模型变更。
3. 是否有官方 `model/main.go` AutoMigrate 变更。
4. 是否有官方 sidebar/header/footer 改动。
5. 分销相关表是否需要独立 migrations，而非完全依赖 AutoMigrate。
6. 生产数据量是否已经要求优化操作记录分页。

常用验证命令：

```bash
go test ./service ./controller ./router
```

```bash
cd web
NODE_OPTIONS=--max-old-space-size=4096 bun run build
```

生产部署前必须：

```bash
sqlite3 /path/to/new-api.db "PRAGMA integrity_check;"
```

并备份数据目录。
